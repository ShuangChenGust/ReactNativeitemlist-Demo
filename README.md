# ReactNativeitemlist-Demo
React native demo for simple item lsit.

This is a simple react-native project for item list. It can realized item add and delete. Flexbox ,flat list, componentes, material UI.

